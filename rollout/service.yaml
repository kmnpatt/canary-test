apiVersion: v1
kind: Service
metadata:
  name: stable-svc
  labels:
    app: nodejs
    service: stable-svc
spec:
  ports:
    - port: 8081
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: nodejs
---
apiVersion: v1
kind: Service
metadata:
  name: canary-svc
  labels:
    app: nodejs
    service: canary-svc
spec:
  ports:
    - port: 8081
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: nodejs