apiVersion: v1
kind: Service
metadata:
  name: stable-svc
  labels:
    app: nginx
    service: stable-svc
spec:
  ports:
    - port: 8081
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: nginx
---
apiVersion: v1
kind: Service
metadata:
  name: canary-svc
  labels:
    app: nginx
    service: canary-svc
spec:
  ports:
    - port: 8081
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: nginx